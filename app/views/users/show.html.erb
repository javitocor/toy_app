<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<p>
  <strong>First Post:</strong>
  <%if @user.microposts.empty? %>
    <p>"No posts yet"</p>
  <% else %>
    <p><%= @user.microposts.first.content %></p>
  <% end %>
</p>

<p><%= link_to 'New Post', new_micropost_path %></p>


<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
